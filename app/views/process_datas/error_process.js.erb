/* Add the new comment to the bottom of the comments list */

$("#error_explanation_process").empty();
$("#error_explanation_process").css("display", "block")
$("#error_explanation_process").append("<p><b><%=_('Please check the following issues before proceding. Thanks!').html_safe%></b></p>");  
  <% full_messages_no_attribute(@process_data.errors).each do |msg| %>    
    $("#error_explanation_process").append("<p><%= msg.html_safe %></p>");
  <% end %>
