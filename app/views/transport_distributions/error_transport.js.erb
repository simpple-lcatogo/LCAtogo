/* Add the new comment to the bottom of the comments list */

$("#error_explanation_transport").empty();
$("#error_explanation_transport").css("display", "block")
$("#error_explanation_transport").append("<p><b><%=_('Please check the following issues before proceding. Thanks!').html_safe%></b></p>");  
  <% full_messages_no_attribute(@transport_distribution.errors).each do |msg| %>    
    $("#error_explanation_transport").append("<p><%= msg.html_safe %></p>");
  <% end %>
